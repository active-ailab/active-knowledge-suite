# Baseline Index

This directory is intended for reusable baseline index artifacts distributed with the source package or release artifact.

Expected contents:

- `manifest.json`
- `config/baseline.yaml`
- `db/metadata.db`
- `vectors/lancedb/`
- `artifacts/`

Large database and vector files may be distributed through Git LFS or a release artifact instead of regular source control.
